require 'rails_helper'

RSpec.describe Restaurant, type: :model do
  # pending "add some examples to (or delete) #{__FILE__}"
  it 'initial validattion' do
    expect(1).to eq(1)
  end

  it 'initial validattion' do
    expect(1).to eq(2)
  end

end
